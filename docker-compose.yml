version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped

  uml:
    image: nginx:alpine
    volumes:
      - ./uml-site:/usr/share/nginx/html:ro
    ports:
      - "8081:80"
    restart: unless-stopped
